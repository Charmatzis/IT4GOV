


@{
    ViewBag.Title = "Εδώ έχει ταυτοποιηθεί ο χρήστης";
}
<h2>@ViewBag.Title</h2>

<div class="panel">
    <h2>Χρήση κάποιου web api service, όπου βρίσκεται σε άλλον server.</h2>
    <br/>
    <a class="btn btn-primary" href="~/home/callapi">Call API</a>
</div>

<div>
    <dl>
        @foreach (var claim in User.Claims)
        {
            <dt>@claim.Type</dt>
            <dd>@claim.Value</dd>
        }
    </dl>
</div>