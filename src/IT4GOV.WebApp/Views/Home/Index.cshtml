@{
    ViewData["Title"] = "Κεντρική";
}

<div class="row">
    <div class="col-sm-10">

        <h1>Εδώ είναι οποιαδήποτε web εφαρμογή όπου χρειάζεται authetication service server side.</h1>
        <br/>
        @if (User.Identity.IsAuthenticated)
        {
            <h2>Είστε συνδεδεμένοι ως: @User.Identity.Name</h2>
        }
        else
        {
            <h2>Δεν είστε συνδεδεμένοι</h2>
        }
        </div>
    </div>
